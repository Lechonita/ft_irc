- un nickname par client, de maximum 9 caracteres
- Channels names are strings (beginning with a '&' or '#' character) of
   length up to 200 characters.  Apart from the the requirement that the
   first character being either '&' or '#'; the only restriction on a
   channel name is that it may not contain any spaces (' '), a control G
   (^G or ASCII 7), or a comma (',' which is used as a list item
   separator by the protocol).
- the client that creates a channel becomes an operator of this channel
- a command can only take up to 15 parameters (that's a lot)
- 512 characters (including \r\n, so really 510) maximum per messages


- tips pour git
	- git rebase ou git merge
	- git checkout a specific commit to detach the HEAD


	Faire le client : utiliser poll() ou pollin() pour savoir si client
	déjà existant ou non. Si pas existant, create client. Sinon, faire
	l'action qu'il veut faire (message).



- nc -C localhost 6667 // pour lancer un client dans le serveur
- Quakenet
- Dalnet : nc -C irc.dal.net 6667


LISTE DES COMMANDES

CHANNEL COMMANDS :
	x JOIN (join channel)
	x PART (leave channel)
	- NAMES (list les utilisateurs de la channel)

 BASIC COMMANDS :
	x PASS (pour rentrer le mdp)
	x NICK (change your nick) //rajouter message d'erreur quand nickname existe deja //supprimer client lorsque QUIT ou controle C
	x USER (set username and mode)
	x PRIVMSG (send a private message to another user)
	- QUIT (quit server)
	- LIST (list channels)
	- WHOIS (get personnal information about a user)

CHANNEL OPERATOR COMMANDS :
	x KICK
	x TOPIC
	x INVITE
	- MODE

QUESTIONS A POSER
	- The command must either be a valid IRC command or a three (3) digit
   number represented in ASCII text.
   -> est-ce qu'on doit gerer ca?

A GERER
	-> quand des users s'envoient des messages dans un channel, ca apparait le nombre de fois qu'il y a de users dans le terminal du serveur
	-> WELCOME apparait a chaque fois que des users s'ecrivent en prive. Ca veut dire que ca passe par le constructor...why ?

   IMPLEMENTER CAP LS (ENVOYE PAR LE CLIENT A LA CONNECTION AU SERVEUR, SERVEUR QUI REPOND PAR CE QU'IL PERMET DE FAIRE)
   EXEMPLE DE Dalnet :
   NETWORK=DALnet SAFELIST MAXBANS=200 MAXCHANNELS=20 CHANNELLEN=32 KICKLEN=307 NICKLEN=30 TOPICLEN=307 MODES=6
          CHANTYPES=# CHANLIMIT=#:20 PREFIX=(ohv)@%+ STATUSMSG=@%+ are available on this server
	CASEMAPPING=ascii WATCH=128 SILENCE=10 ELIST=cmntu EXCEPTS INVEX CHANMODES=beI,k,jl,ciPAmMnOprRsSt
          MAXLIST=b:200,e:100,I:100 TARGMAX=DCCALLOW:,JOIN:,KICK:4,KILL:20,NOTICE:20,PART:,PRIVMSG:20,WHOIS:,WHOWAS: are
          available on this server


:irc 001 JEss :Welcome to the Internet Relay Network JEss!cmoa@127.0.0.1
:irc 002 JEss :Your host is irc, running version 0.6
:irc 003 JEss :This server was created on 2024/01/24
004 JEss :irc 1.0 -none- itkol

BUGS:
	-

INVITE

message automatique sur le terminal de la personne qui invite :
	INVITE Jesson #newtest
	:choopa.nj.us.dal.net 341 Guest05609 Jesson #newtest
	:atw.hu.eu.dal.net NOTICE @#newtest :Guest05609 invited Jesson into channel #newtest


message automatique sur le terminal de l'invité :
	:Guest05609!~lechon@bc22-251f-212f-9745-7c26.210.62.ip INVITE Jesson :#newtest

Jesson = nickname de l'invité
Guest05609 = nickname de ce lui qui invite
newtest = channelname

- changer la commande TOPIC pour qu'elle regarde si le mode t est active ou non dans un channel
- verifier message recu dans le cas ou commande est : PRIVMSG #test42,jess hello (jess et test42 existe et je fais parti de test42)
x verifier les cas ou aucun nom de channel n'est specifier pour JOIN et PART et KICK
x verifier l'absence de nom de clients pour JOIN PART KICK et PRIVMSG
x changer la commande JOIN pour les cas ou y'a une limite d'user dans le channel, un mot de passe, s'il est invite-only
x supprimer les channels dans PART si jamais je suis le dernier utilisateur du channel
x on ne peut pas se KICK soi-meme d'un channel


/connect localhost 1024 lol
/connect localhost 1025 lol



LISTE DE BUGS A CORRIGER

- voir ce qui se passe une fois que le timeout s'est écoulé

- /msg sur irssi > les messages dans un channel s'affichent 2 fois

- Commande CAP > verifier pour le cas irssi où le mdp n'est pas donné

- Quand le timeout est atteint sur irssi, un nouveau user join et ca join tout ! aussi son nickname est celui du pc